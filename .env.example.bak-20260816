# 环境变量覆盖层（可选）。优先级高于 config/settings.json
# 复制为 .env 后填写

# 服务监听
EXPR_HOST=127.0.0.1
EXPR_PORT=3000

# LLM
# LLM_PROVIDER=deepseek
# LLM_API_KEY=

# ASR (FunASR 自部署)
# ASR_WS_URL=ws://host:port
# ASR_TOKEN=